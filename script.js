
const GameBoard = (function() {
    const gameBoard = [];
    return { gameBoard };
})();

const displayControl = (function() {
    
})();

function createPlayer(name) {
    const Username = name;
    return { Username };
}

